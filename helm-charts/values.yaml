# Default values for springboot-demoweb.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

appName: sample

image:
  repository: sarthaksatish/priyankapractice
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  imageTag: 30

canary: "False"
jiraId: test-123

port: 8080
targetPort: 8080

appKind: "microservice"

gatewayName: apps-gateway

virtualServiceEnabled: false

